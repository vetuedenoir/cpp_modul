utilisation des container dans les exercices.

ex00: deque

ex01: Stack ou pile en francais

ex02: vector et list

voici la fonction template que j utilise mais elle n est pas reconnue quand je
l appele dans le main.
(.h)template <typename T> void	merge_insertion_sort(T &container);

(main)std::vector<int> vect;
	std::list<int> liste;
	create_list(vect, liste, av);
	merge_insertion_sort(vect);

(le message d erreur)/usr/bin/ld : obj/main.o : dans la fonction « main » :
main.cpp:(.text+0x165) : référence indéfinie vers « void merge_insertion_sort<std::vector<int, std::allocator<int> > >(std::vector<int, std::allocator<int> >&) »
collect2: error: ld returned 1 exit status
make: *** [Makefile:20 : PmergeME] Erreur 1

peut tu me dire pourquoi cela ne marche pas ?